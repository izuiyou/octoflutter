export enum RenderComparison {
  identical = C.RenderComparison_0,
  metadata = C.RenderComparison_1,
  paint = C.RenderComparison_2,
  layout = C.RenderComparison_3,
}

export enum Axis {
  horizontal = C.Axis_0,
  vertical = C.Axis_1,
}

export enum VerticalDirection {
  up = C.VerticalDirection_0,
  down = C.VerticalDirection_1,
}

export enum AxisDirection {
  up = C.AxisDirection_0,
  left = C.AxisDirection_1,
  down = C.AxisDirection_2,
  right = C.AxisDirection_3,
}

import {LocalizationsDelegate} from '../widgets/localizations'

export class DefaultMaterialLocalizations implements LocalizationsDelegate {
  static delegate: DefaultMaterialLocalizations =
    N.defaultMaterialLocalizationsDelegate()
}

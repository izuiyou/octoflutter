export class DeviceGestureSettings extends N.DeviceGestureSettings {
  constructor(touchSlop?: number) {
    super(touchSlop)
  }
}

// DeviceGestureSettings: function DeviceGestureSettings(t0) {
//     this.touchSlop = t0;
//   },
